#!/bin/bash
# wolfpackrun

set -Eeuo pipefail
set -x

if [ ! -f 'var/wolfpack.sqlite3' ]; then
 ./bin/wolfpackdb create
fi

export FLASK_ENV=development
export FLASK_APP=wolfpack
flask run --host 0.0.0.0 --port 8000
